import 'package:flutter/material.dart';

class LoginPageConin extends StatelessWidget {
  const LoginPageConin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
